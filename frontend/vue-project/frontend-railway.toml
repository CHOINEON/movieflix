# Railway 배포 설정 파일 (Frontend - Vue)

[build]
builder = "NIXPACKS"  # Nixpacks 빌드 시스템 사용

[deploy]
# 프로덕션 빌드 후 프리뷰 서버로 실행
# $PORT는 Railway가 자동으로 할당하는 환경변수
startCommand = "npm run preview -- --port $PORT --host 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
