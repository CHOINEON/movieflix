[phases.setup]
nixPkgs = ['python39']

[phases.install]
cmds = ['cd backend && pip install -r requirements.txt']

[phases.build]
cmds = ['cd backend && python manage.py collectstatic --noinput']

[start]
cmd = 'cd backend && gunicorn movie_project.wsgi --log-file -'
```